def dijkstra_alg(start, unvisited,visited, pathlist, distlist):
    """Using recursive scheme. Generates list of shortest path to every
        node from start point."""
    if unvisited==[]:
        return 
    while adjacent != start: #needs to be integrated with class
    if edge[vert]<distlist[vert]:
        distlist[vert]=edge[vert]
        pathlist[vert]=start
    for 
    dijkstra_alg()

def path_printer(pathlist)
    
def dijkstra(grid,start):
    unvisited=[]
    visited=[]
    pathlist={}
    distlist={}